import { HTMLSkeleton, H1 } from "./global.jsx"

export const snippet_title = "Setter-Getter functions in Unreal Engine 5 C++"
export const snippet_category = "Unreal Engine 5"

<HTMLSkeleton RegisterToSiteGuide={{ category : snippet_category, title : snippet_title }}>

<H1>{snippet_title}</H1>

This will expose the variable `Value` to Blueprints despite being private and each time it is modified in Blueprints it will invoke the setter getter functions

```cpp
public:
    UFUNCTION(BlueprintSetter)
    void SetValue(int NewValue);

    UFUNCTION(BlueprintGetter)
    int GetValue() const;

private:
    UPROPERTY(EditAnywhere, BlueprintSetter=SetValue, BlueprintGetter=GetValue)
    int Value = 10;
```
</HTMLSkeleton>

